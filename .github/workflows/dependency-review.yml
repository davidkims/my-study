name: Hourly Vulnerability Snapshot (OSV Scanner v1.4.3)

on:
  schedule:
    - cron: '0 * * * *'  # 매 시간 정각마다 실행
  workflow_dispatch:

permissions:
  contents: read

jobs:
  osv-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Create Snapshot Directory
        id: snapshot
        run: |
          TIMESTAMP=$(date +"%Y%m%d-%H%M%S")
          SNAPSHOT_DIR=".security-snapshots/slot-${TIMESTAMP}"
          mkdir -p "$SNAPSHOT_DIR"
          echo "SNAPSHOT_DIR=$SNAPSHOT_DIR" >> $GITHUB_ENV
          echo "📸 Created snapshot at $SNAPSHOT_DIR"

      - name: Install OSV Scanner (v1.4.3 - Verified Binary)
        run: |
          curl -sSL https://github.com/google/osv-scanner/releases/download/v1.4.3/osv-scanner-linux-amd64 -o osv-scanner
          chmod +x osv-scanner
          sudo mv osv-scanner /usr/local/bin/
          echo "✅ Installed OSV Scanner at: $(which osv-scanner)"
          osv-scanner --version

      - name: Scan Python dependencies
        continue-on-error: true
        run: |
          if [ -f requirements.txt ]; then
            echo "🔍 Scanning Python requirements.txt..."
            osv-scanner --lockfile=requirements.txt > "${SNAPSHOT_DIR}/osv-python.txt"
          else
            echo "ℹ️ No requirements.txt found"
          fi

      - name: Scan Java dependencies
        continue-on-error: true
        run: |
          if [ -f pom.xml ]; then
            echo "🔍 Scanning Java pom.xml..."
            osv-scanner --lockfile=pom.xml > "${SNAPSHOT_DIR}/osv-java.txt"
          else
            echo "ℹ️ No pom.xml found"
          fi

      - name: Scan Node.js dependencies
        continue-on-error: true
        run: |
          if [ -f package-lock.json ]; then
            echo "🔍 Scanning Node.js package-lock.json..."
            osv-scanner --lockfile=package-lock.json > "${SNAPSHOT_DIR}/osv-node.txt"
          else
            echo "ℹ️ No package-lock.json found"
          fi

      - name: Print Vulnerability Scan Summary
        run: |
          echo "📋 Vulnerability Scan Results:"
          for file in ${SNAPSHOT_DIR}/*.txt; do
            echo "--- $file ---"
            head -n 10 "$file" || echo "(empty)"
          done

      - name: Show Snapshot Directory Tree
        run: |
          echo "🗂 Snapshot Directory Structure:"
          tree .security-snapshots || echo "tree not available"
